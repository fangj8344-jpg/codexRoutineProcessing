<UserControl x:Class="UtilityTools.Modules.FZBPumpController.Views.FZBPumpModelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityTools.Modules.FZBPumpController.Views"
             xmlns:model="clr-namespace:UtilityTools.Modules.FZBPumpController.Model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="数据发送">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <ScrollViewer Grid.Row="0" Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <ItemsControl Grid.Row="0" 
                           ItemsSource="{Binding SendSwitchControlModels}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel LastChildFill="True">
                                        <Label VerticalAlignment="Center"  Width="120" Content="{Binding Name}" DockPanel.Dock="Left">
                                        </Label>
                                        <Button VerticalAlignment="Center" Command="{Binding SendSwitchControlCommand}" DockPanel.Dock="Right">发送控制指令</Button>
                                        <StackPanel Orientation="Horizontal"
                                          HorizontalAlignment="Center">
                                            <TextBlock Text="关" VerticalAlignment="Center" />
                                            <ToggleButton Margin="10,0,10,0"
                                                 IsChecked="{Binding Value, Mode=TwoWay}" />
                                            <TextBlock Text="开" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <ScrollViewer  Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <ItemsControl  ItemsSource="{Binding DataReadbackControlModels}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel LastChildFill="True">
                                        <Label VerticalAlignment="Center"  Width="120" Content="{Binding Name}" DockPanel.Dock="Left">
                                        </Label>
                                        <Button VerticalAlignment="Center" Command="{Binding DataReadbackControlCommand}" DockPanel.Dock="Right">发送控制指令</Button>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock TextAlignment="Center" Width="180"  Text="{Binding Data}"
                                             Margin="2, 0" />
                                        </StackPanel>
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>


                    <ScrollViewer Grid.Row="1" Grid.Column="0"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding DataDeliveryControlModels}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel LastChildFill="True">
                                        <Label VerticalAlignment="Center"  Width="120" Content="{Binding Name}" DockPanel.Dock="Left">
                                        </Label>
                                        <Button VerticalAlignment="Center" Command="{Binding DataDeliveryControlCommand}" DockPanel.Dock="Right">发送控制指令</Button>
                                        <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Center">
                                            <TextBox TextAlignment="Center" Width="210"  VerticalAlignment="Center" Text="{Binding Data}"/>
                                        </StackPanel>
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="数据日志">
                <ScrollViewer >
                    <ListView ItemsSource="{Binding Logs}" />
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
