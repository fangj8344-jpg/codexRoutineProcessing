<UserControl x:Class="UtilityTools.Modules.ImageAnalyzer.Views.ImageOptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UtilityTools.Modules.ImageAnalyzer.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0">
            <TextBlock Text="扫描场大小" />
            <TextBox Text="{Binding ImgMetaData.FieldSize}" />

            <TextBlock Text="放大倍数" />
            <TextBox Text="{Binding ImgMetaData.Zoom}" />

            <TextBlock Text="加速电压" />
            <TextBox Text="{Binding ImgMetaData.AccVoltage}" />

            <TextBlock Text="物镜值" />
            <TextBox Text="{Binding ImgMetaData}" />

            <TextBlock Text="探测器" />
            <TextBox Text="{Binding ImgMetaData.Detector}" />

            <TextBlock Text="机器名称" />
            <TextBox Text="{Binding ImgMetaData.Model}" />

            <TextBlock Text="拍照日期" />
            <TextBox Text="{Binding ImgMetaData.Date}" />
            
            <TextBlock Text="是否拉伸" />
            <CheckBox Text="{Binding ImgMetaData}" />
        </UniformGrid>

        <UniformGrid Grid.Row="1" Rows="1">
            <Image VerticalAlignment="Stretch"
                   HorizontalAlignment="Stretch" />
            <Image VerticalAlignment="Stretch"
                   HorizontalAlignment="Stretch" />
        </UniformGrid>
    </Grid>
</UserControl>
