<UserControl x:Class="UtilityTools.Modules.Test485ChipTool.Views.MainPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityTools.Modules.Test485ChipTool.Views"
             xmlns:localCv ="clr-namespace:UtilityTools.Modules.Test485ChipTool.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <localCv:ColorConverter  x:Key="ColorConverter"/>
    </UserControl.Resources>

    <Grid>
        <TabControl>
            <TabItem Header="测试">
                <StackPanel >
                    <UniformGrid  Grid.RowSpan="2" Grid.ColumnSpan="2" Rows="2" Columns="5" Height="100">
                        <Button Content="串口1测试" Width="100" Command="{Binding TestCommand }" CommandParameter="1"/>
                        <Button Content="串口2测试"  Width="100" Command="{Binding TestCommand}" CommandParameter="2"/>
                        <Button Content="串口3测试" Width="100" Command="{Binding TestCommand}" CommandParameter="3"/>
                        <Button Content="串口4测试"  Width="100" Command="{Binding TestCommand}" CommandParameter="4"/>
                        <Button Content="串口5测试"  Width="100" Command="{Binding TestCommand}" CommandParameter="5"/>
                        <Canvas>
                            <Ellipse Height="40" Width="40" Fill="{Binding ReadySign[0], Mode=TwoWay, Converter={StaticResource ColorConverter}}" Canvas.Left="70" Canvas.Top="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Canvas>
                        <Canvas>
                            <Ellipse Height="40" Width="40" Fill="{Binding ReadySign[1], Mode=TwoWay, Converter={StaticResource ColorConverter}}" Canvas.Left="70" Canvas.Top="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Canvas>
                        <Canvas>
                            <Ellipse Height="40" Width="40" Fill="{Binding ReadySign[2], Mode=TwoWay, Converter={StaticResource ColorConverter}}" Canvas.Left="70" Canvas.Top="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Canvas>
                        <Canvas>
                            <Ellipse Height="40" Width="40" Fill="{Binding ReadySign[3], Mode=TwoWay, Converter={StaticResource ColorConverter}}" Canvas.Left="70" Canvas.Top="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Canvas>
                        <Canvas>
                            <Ellipse Height="40" Width="40" Fill="{Binding ReadySign[4], Mode=TwoWay, Converter={StaticResource ColorConverter}}" Canvas.Left="54" Canvas.Top="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Canvas>
                    </UniformGrid>
                    <DockPanel  Height="50"  LastChildFill="False">
                        <Button Content="一键测试" Command="{Binding  TestCommand}" CommandParameter="0" Height="50"/>

                        <Button Content="重置" Command="{Binding  ClearCommand}" Height="50" DockPanel.Dock="Right"/>
                    </DockPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="日志">
                <ScrollViewer>
                    <TextBlock Text="{Binding Log}"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
