<UserControl x:Class="UtilityTools.Modules.MultiChannelHV.Views.MultiChannelHVContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityTools.Modules.MultiChannelHV.Views"
             xmlns:mm="clr-namespace:UtilityTools.Modules.MultiChannelHV.Model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <DockPanel LastChildFill="False">
                <Button Content="功能初始化" Command="{Binding SetInitCommand}" Margin="10"/>
                <Button Content="隔离板错误清除" Command="{Binding IErrorClearCommand}" Margin="10"/>
                <Button Content="关闭输出" Command="{Binding DisableOutputCommand}" Margin="10"/>
            </DockPanel>
            <DockPanel LastChildFill="False">
                <TextBlock Text="{Binding ReadHV, StringFormat={}当前高压:{0:N0}}" DockPanel.Dock="Left" Margin="10"/>
                <Button Content="设置高压" Margin="10"/>
                <TextBox Text="{Binding WriteHV}"  BorderThickness="0,0,0,2"  DockPanel.Dock="Left" BorderBrush="#CCCCCC" Padding="5,2" MinWidth="100" Margin="10" />
            </DockPanel>
            <ScrollViewer  Height="600"  VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding HVMessageModels}" >
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type mm:HVMessageModel}">
                            <DockPanel LastChildFill="False">
                                <TextBlock Text="{Binding Channel, StringFormat={}{0:N0}通道:}"  Width="100" DockPanel.Dock="Left" Margin="10"/>
                                <TextBlock Text="{Binding ReadHV, StringFormat={}当前隔离电压:{0:N0}}" DockPanel.Dock="Left" Margin="10"/>
                                <Border BorderThickness="2" DockPanel.Dock="Left">
                                    <DockPanel LastChildFill="False">
                                        <Button Command="{Binding SetIVCommand}" Content="设置隔离电压" DockPanel.Dock="Left" Margin="10"/>
                                        <TextBox Text="{Binding WriteHV}"  BorderThickness="0,0,0,2"  DockPanel.Dock="Left" BorderBrush="#CCCCCC" Padding="5,2" MinWidth="100" Margin="10" />
                                    </DockPanel>
                                </Border>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>
