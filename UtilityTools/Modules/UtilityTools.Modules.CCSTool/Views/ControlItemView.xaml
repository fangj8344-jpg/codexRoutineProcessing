<UserControl x:Class="UtilityTools.Modules.CCSTool.Views.ControlItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UtilityTools.Modules.CCSTool.Views"
             xmlns:model="clr-namespace:UtilityTools.Core.Model;assembly=UtilityTools.Core"
             xmlns:attach="clr-namespace:UtilityTools.Core.Attach;assembly=UtilityTools.Core"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="CCSItemTemplate"
                      DataType="{x:Type model:IntSliderInfoModel}">
            <DockPanel LastChildFill="True"
                       VerticalAlignment="Center">
                <Label Content="{Binding Title}"
                       HorizontalContentAlignment="Center"
                       VerticalContentAlignment="Center"
                       DockPanel.Dock="Left" />
                <TextBox Text="{Binding Value}"
                         HorizontalContentAlignment="Center"
                         VerticalContentAlignment="Center"
                         DockPanel.Dock="Right" />
                <Slider Value="{Binding Value}"
                        Minimum="{Binding MinValue}"
                        Maximum="{Binding MaxValue}"
                        DockPanel.Dock="Left"
                        attach:SliderAttached.IsSupportWheel="True" />
            </DockPanel>
        </DataTemplate>
    </UserControl.Resources>

    <DockPanel LastChildFill="True">
        <Label Content="{Binding Title}"
               HorizontalContentAlignment="Center"
               VerticalContentAlignment="Center"
               DockPanel.Dock="Top" />
        <ItemsControl  ItemsSource="{Binding ControlItems}"
                       ItemTemplate="{StaticResource CCSItemTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </DockPanel>
</UserControl>
