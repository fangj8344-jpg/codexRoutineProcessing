<UserControl x:Class="UtilityTools.Modules.MultiAxisTest.Views.MultiAxisScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             InputMethod.IsInputMethodEnabled="False"
             d:DesignHeight="550" d:DesignWidth="1000">

    <md:DialogHost Identifier="MultiAxisScanViewHost" DialogTheme="Inherit">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <md:PackIcon Kind="BarcodeScan" Width="36" Height="36" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                <TextBlock Text="样品台扫码自动识别" Style="{StaticResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Margin="16,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*"/>
                    <ColumnDefinition Width="4*"/>
                </Grid.ColumnDefinitions>

                <md:Card Grid.Column="0" Margin="0,0,12,0" Padding="24" UniformCornerRadius="8" md:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="解析详情" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <md:PackIcon Grid.Row="0" Grid.Column="0" Kind="CalendarClockOutline" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="电镜型号" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.ElectronMicroscopeModel}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                            <md:PackIcon Grid.Row="1" Grid.Column="0" Kind="CalendarClockOutline" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="样品台型号" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.StageType}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                            <md:PackIcon Grid.Row="2" Grid.Column="0" Kind="CartOutline" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="采购订单号" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.PurchaseOrder}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                            <md:PackIcon Grid.Row="3" Grid.Column="0" Kind="Factory" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="生产订单号" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.ProductionOrder}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                            <md:PackIcon Grid.Row="4" Grid.Column="0" Kind="AccountHardHat" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="操作 / 检验员工号" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.OperatorId}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                            <md:PackIcon Grid.Row="5" Grid.Column="0" Kind="CalendarClockOutline" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="生产日期" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="5" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.ProductionDate}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                            <md:PackIcon Grid.Row="6" Grid.Column="0" Kind="Barcode" Width="24" Height="24" Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="序列号" Foreground="Gray" FontSize="15" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="6" Grid.Column="2" Text="{Binding State.CurrentScanDisplay.SerialNumber}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryHueMidBrush}"/>
                        
                        </Grid>
                    </StackPanel>
                </md:Card>

                <md:Card Grid.Column="1" Margin="12,0,0,0" Padding="24" UniformCornerRadius="8" md:ElevationAssist.Elevation="Dp2">
                    <StackPanel>
                        <TextBlock Text="原始扫码数据" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,20"/>

                        <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Text="{Binding State.CurrentScanText}" 
                                 md:HintAssist.Hint="请使用扫码枪扫描条码..."
                                 md:HintAssist.IsFloating="True"
                                 IsReadOnly="True" 
                                 Focusable="False" 
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Height="100"
                                 FontSize="16"
                                 Margin="0,10,0,0"/>

                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center" Opacity="0.6">
                            <md:PackIcon Kind="InformationOutline" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="请确保输入法处于纯英文状态" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </md:Card>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Command="{Binding ConfirmCommand}"
                        Width="220" Height="46"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        md:ButtonAssist.CornerRadius="23"
                        ToolTip="验证通过后点击进入测试">
                    <StackPanel Orientation="Horizontal">
                        <md:PackIcon Kind="CheckCircleOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="确认并进入测试" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </md:DialogHost>
</UserControl>