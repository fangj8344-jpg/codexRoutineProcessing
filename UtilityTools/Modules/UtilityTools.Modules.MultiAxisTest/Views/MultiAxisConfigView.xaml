<UserControl x:Class="UtilityTools.Modules.MultiAxisTest.Views.MultiAxisConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="多轴测试配置"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- 左：两轴 Zem18/Zem20 -->
            <md:Card Grid.Column="0" Margin="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="两轴电机" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                    <RadioButton Content="Zem18" GroupName="MotorKind"
                                 IsChecked="{Binding IsTwoAxisZem18}" Margin="0,0,0,6"/>
                </StackPanel>
            </md:Card>
            <md:Card Grid.Column="1" Margin="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="两轴电机" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                    <RadioButton Content="Zem20" GroupName="MotorKind"
                      IsChecked="{Binding IsTwoAxisZem20}" Margin="0,0,0,6"/>
                </StackPanel>
            </md:Card>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button Content="下一步：扫码"
                    Width="160" Height="36"
                    Style="{StaticResource MaterialDesignFlatMidBgButton}"
                    Command="{Binding NextCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>

