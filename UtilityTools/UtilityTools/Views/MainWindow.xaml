<Window x:Class="UtilityTools.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:core="clr-namespace:UtilityTools.Core;assembly=UtilityTools.Core"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="微软雅黑"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        AllowsTransparency="False"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialDesignWindow}"
        Title="{Binding Title}"
        Height="800"
        Width="1280">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedWindowCommand}"
                                   PassEventArgsToCommand="True" />
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding ClosingWindowCommand}"
                                   PassEventArgsToCommand="True" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <md:DialogHost Identifier="Root"
                   DialogTheme="Inherit"
                   x:Name="DialogHost">
        <md:DrawerHost x:Name="drawerHost"
                       IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">

            <!--左侧抽屉滑窗-->
            <md:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="220">
                    <ListBox x:Name="menuBar"
                             Margin="0, 100"
                             DockPanel.Dock="Top"
                             ItemsSource="{Binding Modules, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             SelectedIndex="{Binding SelectedIndex}"
                             ScrollViewer.VerticalScrollBarVisibility="Hidden"
                             ItemContainerStyle="{StaticResource MyListBoxItemStle}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding NavigateCommand}"
                                                       CommandParameter="{Binding ElementName=menuBar, Path=SelectedItem}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <md:PackIcon Kind="{Binding Icon}"
                                                 Margin="15, 0" />
                                    <TextBlock Text="{Binding Title}"
                                               Margin="10, 0"
                                               TextAlignment="Center"
                                               ToolTip="{Binding Tip}">
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </md:DrawerHost.LeftDrawerContent>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <md:Snackbar x:Name="Snackbar"
                             Grid.Row="0"
                             Panel.ZIndex="1"
                             MessageQueue="{md:MessageQueue}" />

                <!--顶部-->
                <md:ColorZone x:Name="ColorZone"
                              Grid.Row="0"
                              Padding="10"
                              md:ElevationAssist.Elevation="Dp4"
                              Mode="PrimaryMid">
                    <DockPanel LastChildFill="false">
                        <StackPanel DockPanel.Dock="Right"
                                    Orientation="Horizontal">
                            <Button x:Name="btnMin"
                                    Content="—"
                                    Style="{StaticResource MaterialDesignFlatMidBgButton}"
                                    Cursor="Hand" />
                            <Button x:Name="btnMax"
                                    Content="☐"
                                    Style="{StaticResource MaterialDesignFlatMidBgButton}"
                                    Cursor="Hand" />
                            <Button x:Name="btnClose"
                                    Content="✕"
                                    Style="{StaticResource MaterialDesignFlatMidBgButton}"
                                    Cursor="Hand" />
                        </StackPanel>


                        <StackPanel DockPanel.Dock="Left"
                                    Orientation="Horizontal">
                            <ToggleButton x:Name="MenuToggleButton"
                                          Style="{StaticResource MaterialDesignHamburgerToggleButton}" />
                            <Button ToolTip="Previous View"
                                    Margin="10,0,0,0"
                                    Command="{Binding GoBackCommand}"
                                    Style="{StaticResource MaterialDesignToolButton}"
                                    Content="{md:PackIcon Kind=ArrowLeft,Size=24}"
                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}" />
                            <Button ToolTip="Next View"
                                    Margin="10,0,0,0"
                                    Command="{Binding GoForwardCommand}"
                                    Style="{StaticResource MaterialDesignToolButton}"
                                    Content="{md:PackIcon Kind=ArrowRight,Size=24}"
                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}" />
                            <Button ToolTip="Home View"
                                    Margin="10,0,0,0"
                                    Command="{Binding GoHomeCommand}"
                                    Style="{StaticResource MaterialDesignToolButton}"
                                    Content="{md:PackIcon Kind=Home,Size=24}"
                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}" />
                        </StackPanel>


                        <TextBlock Text="{Binding Title}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="20"
                                   DockPanel.Dock="Top" />
                    </DockPanel>
                </md:ColorZone>

                <!--主体内容区域-->
                <ContentControl Grid.Row="1"
                                prism:RegionManager.RegionName="{x:Static core:RegionNames.MainWindowRegionName}" />
            </Grid>

        </md:DrawerHost>
    </md:DialogHost>
</Window>
